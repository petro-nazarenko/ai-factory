[project]
name = "workspace"
version = "0.3.0"
description = "AI Factory workspace utilities"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["workspace*"]
